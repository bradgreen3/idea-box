<h1> All Categories </h1>

<% @categories.each do |category| %>
<ul class=category_<%= category.id %>>
  <%= category.name %>
  <br>
  <%= link_to "Delete", admin_category_path(category), method: "delete" %>
  <br>
  <br>
</ul>
<% end %>
